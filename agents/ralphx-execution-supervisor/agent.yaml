name: ralphx-execution-supervisor
role: supervisor
description: "Monitors task execution and intervenes when problems occur"
capabilities:
  mcp_tools: []
harnesses:
  claude:
    model: sonnet
    tools:
      extends: base_tools
      include:
        - Task
    preapproved_cli_tools:
      - Bash
      - Task(Explore)
      - Task(Plan)
