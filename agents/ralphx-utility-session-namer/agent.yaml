name: ralphx-utility-session-namer
role: session_namer
description: "Generates concise titles for ideation sessions and agent conversations based on the initial user context"
capabilities:
  mcp_tools:
    - update_session_title
harnesses:
  claude:
    model: sonnet
    tools:
      mcp_only: true
    preapproved_cli_tools: []
