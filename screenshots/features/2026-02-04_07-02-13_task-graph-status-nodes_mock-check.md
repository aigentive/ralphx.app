TaskGraph status nodes mock check

- View: Graph (web mode)
- Data: task graph nodes + status colors from mock task data
- Result: graph renders with status styling and legend without missing mock data
