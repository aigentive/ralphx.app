> **Maintainer note:** Keep this file compact. Prefer one-line rules, links to source docs, and explicit non-negotiables over prose.

# AGENTS.md

## Project
RalphX — native Mac GUI for autonomous AI development with Rust/Tauri backend and React frontend.

Primary project docs:
- `CLAUDE.md`
- `src-tauri/CLAUDE.md`
- `.claude/rules/*.md`
- `.claude/rules/multi-harness.md` for provider-neutral runtime/config/event rules and documentation sync requirements
- `.claude/rules/agent-mcp-tools.md` for multi-layer agent MCP/tool alignment across prompt frontmatter, `ralphx.yaml`, and MCP allowlists
- `.claude/rules/rust-test-execution.md` for selective Rust test commands, the standard Rust test stack, shared SQLite fixtures/builders, and the no-broad-`fmt` rule

## Codex Rules

| Rule | Detail |
|---|---|
| Read project instructions first | Before substantial work, read the relevant repo docs (`CLAUDE.md` at root, plus subtree docs like `src-tauri/CLAUDE.md`). |
| Preserve user work | Never revert unrelated changes you did not make. If the tree is dirty, isolate your edits and verify diffs before commit. |
| Legacy harness compatibility (NON-NEGOTIABLE) | Pre-Codex persisted data and payloads must keep working. Provider-neutral changes stay additive/derivable from legacy Claude-only fields until an explicit migration/removal plan lands. |
| Minimal diffs | Avoid formatter churn and accidental refactors. Keep changes scoped to the task. |
| Agent tool alignment | MCP/tool changes are multi-layer: keep prompt frontmatter, `ralphx.yaml`, and MCP allowlists in sync. Source: `.claude/rules/agent-mcp-tools.md`. |
| Handler module split | Oversized Rust HTTP handlers belong in directory-backed modules (`foo/mod.rs` + endpoint-family files), not single multi-thousand-line `foo.rs` files. |
| Mechanical extraction only (NON-NEGOTIABLE) | Large code moves/splits must use real move/extract operations (`mv`, `sed`, `awk`, scripted extraction). Do not hand-copy or retype large existing bodies into new files. Source: `.claude/rules/code-quality-standards.md`. |
| Apply-patch is fix-up only (NON-NEGOTIABLE) | After a mechanical move, `apply_patch` is only for the small follow-up layer: imports, visibility, re-exports, module wiring, tests. It is not a substitute for moving existing code. |
| Mechanical split recovery | If a large extraction drifts into patch-copying or visibility churn, restore the module to `HEAD`, clean any parked WIP out of the repo tree, and redo the split mechanically; do not keep iterating on a half-split tree. |
| Rustfmt scope safety | Never run `rustfmt` on Rust module roots like `mod.rs` unless the user explicitly wants recursive formatting; it can rewrite child modules and create unrelated churn. |
| Rustfmt edition safety | `src-tauri/Cargo.toml` is on Rust edition `2021`; for surgical PR work prefer `rustfmt --edition 2021 <leaf-file.rs>` and avoid plain `cargo fmt` unless the change intentionally wants broad repo-wide formatting. |
| Cargo during refactors | Never overlap Cargo jobs while validating a large extraction; one targeted run at a time or you recreate build-lock noise and lose signal. |
| Rust test runner split | Use `cargo test` for selective filters and doctests; use `cargo nextest run` for broad Rust lib runs. CI runs both. Details: `.claude/rules/rust-test-execution.md`. |
| Rust toolchain source of truth | `rust-toolchain.toml` is authoritative; use a `rustup`-managed toolchain so the repo pin actually applies. |
| Rust std API stability (NON-NEGOTIABLE) | Do not use unstable std APIs in production Rust (example: `is_multiple_of`). Use stable equivalents such as `%` with a zero guard where needed. Source of truth: `.claude/rules/rust-stable-apis.md`. |
| Worktree safety (NON-NEGOTIABLE) | In Worktree mode, task/review flows must never silently fall back to the user’s main repo checkout. Prefer hard failure and repair/self-heal paths. |
| Verify before commit | Review `git diff` for every file you touched against `HEAD` and confirm only intended hunks remain before committing. |
| Refactor tracker hygiene | When any agent notices a high-value architectural/refactor hotspot, update `## High-Value Refactor Targets` in this file in the same slice; do not leave major debt only in chat context. |
| Turn-level refactor discipline (NON-NEGOTIABLE) | On every substantial turn, if the touched slice reveals repeated construction/wiring/branching across production callsites, either centralize it in that turn or explicitly add/promote it in `## High-Value Refactor Targets` before continuing feature work. |
| Factory-first runtime wiring | When the same scheduler/chat/transition/runtime assembly appears in 3+ production callsites, stop adding another copy: extend a shared builder/factory (`AppState` or dedicated helper) and migrate callsites instead. |
| Future harness readiness | Multi-harness changes should prefer provider-neutral registries/factories keyed by `AgentHarnessKind` over one-off `claude+codex` branching whenever that abstraction can be landed safely in the current slice. |

## Backend

When working in `src-tauri/`, also follow:
- `src-tauri/CLAUDE.md`
- `.claude/rules/multi-harness.md`
- `.claude/rules/rust-stable-apis.md`
- `.claude/rules/rust-test-execution.md`
- `.claude/rules/task-git-branching.md`
- `.claude/rules/code-quality-standards.md`
- `.claude/rules/agent-mcp-tools.md`

## Optimization Tracking

| Priority | Area | Next Step |
|---|---|---|
| P0 | Global orchestration semantics | Landed: persisted halt mode, explicit stopped UI state, stopped-aware control bar copy, `resume_execution` rejection after stop, queued-message pressure surfaced in execution status/control-bar telemetry, and inline execution-bar warning treatment for held agent messages; next observe whether thresholding or stronger escalation is needed |
| P0 | Concurrency admission control | Landed: project-aware slot admission now applies across chat spawns, scheduler selection, low-level spawner dispatch, startup recovery, merge-retry scheduling, manual task resume, paused ideation queue resume fairness across projects, paused task/review/merge queue fairness across projects, reconciliation auto-resume/retry paths, mixed ideation-vs-review/merge contention tests, and combined mixed-load resume regressions across both queue families; next keep broadening contention coverage where real queue pressure revealed gaps |
| P0 | Pipeline allocation | Ideation spawn/resume now respect global cap, project ideation cap, project total cap, borrowing only when execution is not waiting, cross-project paused-queue fairness during resume, and execution-side queued work now gets first claim on shared capacity before ideation relaunch; task/review/merge admission, manual task resume, reconciliation retries, and spawner dispatch all respect project totals across runtime + recovery paths; next validate under heavier mixed load and decide whether queued-message pressure needs stronger inline UX than the current telemetry |
| P1 | Queue + recovery alignment | Landed: pause queues all slot-consuming sends, resume relaunches paused ideation + active task/review/merge queued work in execution-first order, stop clears queued slot-consuming work, and execution status now reports queued agent-message pressure separately from Ready-task queue depth; next validate whether more real-time queue event emission is worth the extra plumbing |
| P1 | Settings surface | YAML-seeded defaults plus UI/API controls now cover global/project ideation caps + borrow toggle; next keep validating those controls against per-project admission and borrowing behavior |
| P1 | Autonomous scope drift prevention | Core path landed: proposal scope hints, verifier pressure, review drift detection, revise-first routing, follow-up provenance/dedupe, merge backstop, cross-project lineage; next watch real runs and extract the pure review/scope/follow-up logic out of the root crate without weakening adaptive scope expansion |
| P1 | Build / compile-coupling reduction | Landed slices: shared review scope-drift matching + blocker-fingerprint logic, `complete_review` tool schema/validation, review follow-up prompt / issue-normalization helpers, review decision/policy validation, review outcome/result shaping, review follow-up / scope-metadata shaping, review issue mapping, review history-selection helpers, merge scope-backstop evaluation, child-session matching/construction helpers, execution-status counting/running-status matching, running-process/ideation view shaping, execution status/command response shaping, and task-context artifact preview/hint shaping now live in `ralphx-domain`; verification-child creation now reuses that shared child-session path too; next keep moving high-churn pure backend decision code out of the heavy `src-tauri` crate so every validation run rebuilds less |
| P1 | `src-tauri/src/lib.rs` composition root | `lib.rs` still owns app setup, startup recovery, background workers, shutdown, and command registration; landed slices: shutdown moved behind `application::shutdown`, invoke-handler registration moved to `commands::registry`, shell/runtime wiring moved to `application::runtime_wiring`, startup background helpers moved to `application::startup_background`, startup transition reconstruction moved to `application::startup_transition_factory`, scheduler/chat-resumption/reconciliation builder assembly moved to `application::startup_runtime_builders`, the remaining startup recovery flow moved to `application::startup_pipeline`, process/bootstrap logging + dotenv setup moved to `application::startup_bootstrap`, the full Tauri `.setup(...)` orchestration moved to `application::app_setup`, and that setup module already split into `application::setup_settings` + `application::startup_cleanup` + `application::server_boot` + `application::startup_pipeline_launch`; next keep `run()` and `app_setup()` as thin composition roots and push any new startup logic into focused support modules instead of letting either grow again |
| P2 | Transition handler support layer | After concurrency semantics stabilize, resume splitting `merge_validation`, `merge_coordination`, remaining `side_effects` hot spots, the oversized review/freshness corrective-routing path across `src-tauri/src/application/task_transition_service.rs` plus `src-tauri/src/domain/state_machine/transition_handler/on_enter_states/review.rs`, and the growing merge gate stack in `src-tauri/src/domain/state_machine/transition_handler/side_effects/merge_attempt.rs` |
| P2 | Scheduler + watchdog orchestration | `src-tauri/src/application/task_scheduler_service.rs` now carries Ready scheduling, deferred-merge retries, main-merge retries, contention retry logic, and parked-review freshness wakeups; next extract retry families/watchdog querying into smaller support modules once the current semantics settle |
| P2 | Execution command orchestration | Split `src-tauri/src/commands/execution_commands.rs` after the transition-handler support layer pass; priority slices are pause/stop/resume orchestration, queue relaunch helpers, status/event payload shaping, and the oversized embedded test block |
| P2 | Capability test split | Continue moving OS-capability checks out of default broad suites into explicit ignored tests or dedicated capability binaries |
| P3 | Oversized HTTP handlers | After transition-handler stabilization, resume large backend handler refactors like `git.rs` and `teams.rs` |

## Active Reliability Tracker

| Priority | Stream | Next Step |
|---|---|---|
| P0 | Verifier critic resumption protocol | Landed: `ralphx-plan-verifier` now treats `Task(...)` results with `agentId` as resumable/in-progress, requires full-context rescue prompts, placeholder artifact session ids are rejected, and verification-child artifact read/write calls are auto-remapped to the parent ideation session; next add deeper regressions and, if needed, runtime-side recovery so critics do not false-escalate while artifacts are still pending |
| P0 | Model-agnostic MCP/tool UX | Landed: high-friction MCP tools now document parent-vs-child session rules plus concrete payload examples, child-status / session-message tools now show verification-debug usage explicitly, the MCP proxy now appends tool-specific repair hints/examples on backend errors, the verifier path now has narrower `report_verification_round` / `complete_plan_verification` helpers plus `get_verification_round_artifacts`, and verification/artifact endpoints now auto-remap verification-child session ids to the parent where the mapping is deterministic; next extend that repair-oriented treatment to more brittle workflows |
| P0 | Startup external-session archival safety | Landed: cold boot now respects the external-session TTL, startup recovery explicitly hands claimed Phase N+1 ideation session ids into cold-boot archival, and verified external sessions are preserved; next observe real runs before widening recovery-specific heuristics further |

## Active Transition Hardening Tracker

| Priority | Stream | Next Step |
|---|---|---|
| P0 | Validated task transitions by default | Landed first slice: `TaskTransitionService::transition_task*` now rejects illegal `from -> to` pairs via `InternalStatus::can_transition_to`, same-status calls remain no-op, and invalid attempts log task id + from/to before any persistence/history/events/side effects; next audit the remaining validated callers for any workflow paths that were implicitly relying on permissive direct writes |
| P0 | `transition_task*` caller audit and migration | Investigated: the production `transition_task()` / `transition_task_with_metadata()` surface is mapped across review handlers, git/merge handlers, external task actions, chat-service flows, reconciliation, scheduler/execution commands, and project/task commands; landed direct handler coverage for `reviews/human.rs` and `git.rs`, command-side status-guard helpers now lock the Tauri review/git command preconditions in `commands/review_commands.rs` and `commands/git_commands.rs`, merge reconciler coverage now locks the `branch_missing` skip guards plus explicit `MergeIncomplete -> PendingMerge` transition history for mode-switch, validation-retry, starvation-guard, and transient-git retry branches and explicit `MergeConflict -> PendingMerge` history for the cooldown-expired retry branch, `resume_execution` no longer invents `Executing` on malformed pause metadata because it falls back to real pause history, chat-service completion helpers now lock the runtime targets for execution-complete (`PendingReview` vs `Failed`) and incomplete-review exit (`Escalate` vs shutdown skip vs ignore-already-transitioned), `reconciliation_runner.rs` now locks the completed-execution fallback path that advances `Executing` tasks into `PendingReview`/`Reviewing` when the finished run is discovered via task-execution conversation history rather than state-history metadata, and `pr_poller_tests.rs` now locks both the happy `Merging -> Merged` path and the stale-poller guard that must not regress an already-`Merged` task back to `MergeIncomplete`; next highest-signal remaining validated caller is now mostly the deeper merge runtime/recovery branches that still depend on entry-action side effects after `PendingMerge` re-entry |
| P0 | Corrective-path separation | Landed more slices: `transition_task_corrective()` now exposes the explicit nonstandard path for command-side repair flows without weakening the default validator, `reject_fix_task` now uses that corrective path for `Blocked -> Failed` and original-task backlog fallback, and `ReviewService` mirrors the same validated/corrective split through repo-backed helpers; next keep documenting that normal workflow callers must not use corrective transitions and only preserve direct repo writes where they are the canonical engine/bootstrap path |
| P0 | Direct task status mutation audit | Landed more slices: `answer_user_question` now routes through `TaskTransitionService`, `update_task` now rejects status edits so generic task editing can no longer act as a hidden state-transition backdoor, `approve_fix_task` now uses the validated `Blocked -> Ready` transition path, command-side `reject_fix_task` now uses explicit corrective transitions, `ReviewService` now persists its fix-task/backlog/reject flows through small validated/corrective repo-backed helpers instead of silent raw writes, and `startup_jobs.rs` now funnels crash-recovery/unblock/dependency-reconciliation persistence through a local optimistic-lock + history helper rather than repeated direct `update()` blocks; current classification: merge internals (`merge_completion.rs`, `merge_outcome_handler.rs`, `side_effects/*`) remain the canonical state-machine engine with their own freshness checks/history/events, remaining review-side raw writes are limited to task creation/bootstrap defaults for new fix tasks, and the notable non-engine writes in `commands/plan_branch_commands.rs`, `commands/ideation_commands/ideation_commands_apply.rs`, and `application/apply_service/mod.rs` are now classified as bootstrap-only task/merge-task creation or initial dependency-graph assignment rather than live workflow transitions; next only revisit that bucket if one of those helpers starts mutating already-active tasks after creation |
| P1 | Branchless terminal semantics | Track separately from transition hardening: branchless / no-merge tasks currently reuse `merged` as the dependency-satisfying terminal even when nothing was actually merged; first slice now mirrors the existing branchless/no-code skip in `TaskTransitionService`, but the semantic question remains open; next evaluate whether these tasks should keep the current `merged` semantics for workflow compatibility or move to a distinct “approved/finalized without merge” terminal model with updated dependency-satisfaction and UI rules |
| P1 | Invalid-transition observability | Landed first slice: `TaskTransitionService` now captures `#[track_caller]` file/line/column on normal transition attempts, invalid-transition rejections, optimistic-lock skips, confirmed transitions, and corrective-transition conflicts, so future illegal callers are traceable without threading a repo-wide caller enum; next decide whether user-facing error payloads also need caller/context in addition to logs |
| P1 | Docs / agent-rule alignment | Landed: `CLAUDE.md`, `src-tauri/CLAUDE.md`, and the task state/review/view rules now document the validated-transition contract, explicit corrective-only bypasses, the preserved `reviewing -> review_passed -> approved` no-human-review path, and the still-open branchless terminal-semantics question; next keep those docs in sync if the branchless model or corrective surface changes again |
| P1 | Transition-hardening regression coverage | Landed slices: core `TaskTransitionService` tests now lock `reviewing -> approved` and `merged -> approved` rejection, external `approve_review` regressions remain green, review handler coverage now locks the branchless no-human-review approval path plus human-review `approve_task`/`request_task_changes` entry points, git handler coverage now locks `complete_merge` idempotence vs invalid-state rejection, review/git command guard helpers now have direct unit coverage for approvable/re-review/retry/resolve state gates, merge reconciler coverage now locks `branch_missing` as a hard skip for `MergeIncomplete` / `MergeConflict` and now also proves the mode-switch, validation-retry, starvation-guard, and transient-git retry branches all record `MergeIncomplete -> PendingMerge` while the cooldown-expired conflict retry records `MergeConflict -> PendingMerge`, execution lifecycle coverage now locks malformed pause metadata to history-or-skip rather than hardcoded `Executing`, chat-service handler coverage now locks execution completion + incomplete-review exit classification through dedicated helper tests, reconciliation-runner coverage now locks the finished-run `Executing -> PendingReview` recovery path via the real task-execution conversation lookup, and PR poller coverage now locks both the successful `Merging -> Merged` path and rejection of stale `Merged -> MergeIncomplete` regressions; next broaden caller-surface regressions only where validated production transitions still lack direct handler/command tests, especially the remaining merge recovery paths that rely on validated transitions rather than canonical engine writes |

## Active Migration Tracker

| Stream | Goal | Status | Notes |
|---|---|---|---|
| Frontend relocation | Move the Vite/React app from repo root into `frontend/` while keeping `src-tauri/` at root | Completed | `package.json`, configs, `public/`, `src/`, and `tests/` now live under `frontend/` |
| Tauri wiring | Repoint Tauri dev/build hooks at `frontend/` and make frontend-local `npm run tauri ...` work | Completed | `frontend/package.json` shells back to repo root for the Tauri CLI; `src-tauri/tauri.conf.json` now points at `../frontend` |
| Repo command surface | Update docs/scripts/CI/release flows from root-frontend assumptions to `frontend/` commands | Completed | README, DEVELOPMENT, getting-started, build/release/CI paths rewired to `frontend/` |
| Tooling path refs | Update Claude/Cursor/rule-manager/path-scoped rule references from `src/**` and `tests/**` to `frontend/src/**` and `frontend/tests/**` where repo-local paths matter | Completed | `.claude/settings.json`, visual testing/api rules, and rule-manager scripts adjusted |
| Root cleanup | Remove stale root artifacts and reduce visible clutter | Completed | `rollback_backup.json` and empty `.config/` removed; local generated dirs now live under `.artifacts/`; `.cursor/` intentionally kept at root |
| Artifact strategy | Move generated local outputs under `.artifacts/` while keeping Playwright visual baselines tracked in-repo | Completed | `logs/`, `reports/`, `screenshots/`, and `backups/` moved under `.artifacts/`; frontend visual baselines stay in `frontend/tests/visual/snapshots/` |
| Asset publishing pipeline | Split ignored source captures from tracked public assets, move asset tooling under `assets/scripts/`, and add reusable compression/publish commands | Completed | `assets/raw/` is now gitignored source, `assets/public/` is the tracked publish set, `assets/scripts/` owns framing/diagram/compression, and `.claude/rules/assets.md` holds the workflow |
| Plugin namespace cleanup | Move the shared plugin under `plugins/shared` now; keep the heavier `ralphx-plugin` move as a later dedicated refactor | Completed | `ralphx-shared-plugin/` now lives at `plugins/shared/`; plugin name/namespace stays `ralphx-shared-plugin` for marketplace and slash-command stability |
| App plugin migration plan | Move the app plugin from `ralphx-plugin/` to `plugins/app/` without breaking runtime plugin discovery, packaging, or docs | Completed | Runtime fallback landed first, then the tree/config/docs/release path move landed with targeted validation |
| Validation | Re-run targeted frontend and Tauri-facing checks after rewiring completes | Completed | `npm --prefix frontend run typecheck`, `npm --prefix frontend run lint`, and `npm --prefix frontend run tauri build -- --help` succeeded |

## App Plugin Migration Tracker

| Stream | Goal | Status | Notes |
|---|---|---|---|
| Target naming | Finalize `ralphx-plugin -> plugins/app` as the repo path while keeping agent/plugin semantics clear | Completed | `plugins/app` won over `plugins/core`; the plugin contains agents, hooks, internal MCP, and external MCP, so `app` matches the actual role better |
| Runtime path resolution | Update backend/plugin-dir discovery from `./ralphx-plugin` to `./plugins/app` without breaking worktree/dev/prod fallback logic | Completed | Claude runtime now prefers `plugins/app`, keeps `ralphx-plugin` as a fallback, routes teammate spawns through the shared resolver, and updates default plugin-dir surfaces before the tree move |
| Config surface | Rewrite plugin paths in `ralphx.yaml` and path-scoped Claude rules/docs | Completed | Mechanical path rewrites landed; plugin ids and agent names stayed unchanged |
| Packaging + release | Update release/build scripts and production app-data plugin install path to the new location | Completed | Release provisioning now copies from `plugins/app` into `~/Library/Application Support/com.ralphx.app/plugins/app` |
| Docs + rules | Rewrite architectural/docs references after runtime/config changes are stable | Completed | Docs/rules now point at `plugins/app`; only explicit legacy fallback explanations still mention `ralphx-plugin` |
| Validation | Run targeted runtime/config checks after the move and only then commit | Completed | `cargo test --manifest-path src-tauri/Cargo.toml test_resolve_plugin_dir_ --lib`, `cargo test --manifest-path src-tauri/Cargo.toml test_plugin_repo_root_supports_nested_plugins_app_layout --lib`, `cargo test --manifest-path src-tauri/Cargo.toml test_teammate_spawn_config_new_defaults --lib`, `cargo test --manifest-path src-tauri/Cargo.toml -p ralphx-domain test_agent_config_default`, `cargo test --manifest-path src-tauri/Cargo.toml test_all_system_prompt_files_exist --lib`, and `bash -n scripts/build-release.sh scripts/count-loc.sh plugins/app/skills/rule-manager/scripts/rule-suggest-paths.sh` passed |

## Codex CLI Project Tracker

| Priority | Stream | Status | Next Step |
|---|---|---|---|
| P0 | Cross-session source of truth | Started | Keep this tracker, `specs/codex-cli.md`, `docs/ai-docs/codex-cli/`, `docs/ai-docs/reefagent-codex-cli.md`, and `docs/ai-docs/reefagent-codex-responses.md` updated together so Codex parity research survives across sessions |
| P0 | Discovery PR checkpoint | Completed | Open PR `#17` (`feat: add codex cli harness foundation`) from `codex-cli-discovery` so ongoing implementation planning can build on a reviewable baseline |
| P0 | Vendor doc snapshot | Started | Maintain a local Codex CLI doc index under `docs/ai-docs/codex-cli/` covering CLI, config, approvals/security, sandboxing, AGENTS/rules/hooks, MCP, skills, subagents, models, auth, and non-interactive mode; note version skew between installed CLI help and current official docs |
| P0 | Harness abstraction | In progress | Landed provider-neutral spawn metadata, Codex exec/resume builders, MCP config override injection, lane-aware queue resume branching, Codex JSONL stream parsing, and canonical provider-session reads in the Claude builder/runtime paths; next complete unified startup/reconciliation and consumer normalization |
| P0 | Core milestone | In progress | Ideation lane resolution now permits Codex, send/resume paths branch to Codex `exec` / `exec resume`, Codex runs operate in explicit solo mode, and sparse Codex lane rows now fall back to phase-1 `gpt-5.4` / `gpt-5.4-mini` defaults instead of leaking legacy Claude model names; next validate end-to-end ideation + verification execution and recovery |
| P0 | Config/settings matrix | In progress | Backend lane settings storage plus the ideation harness settings UI now support global/project harness selection for the four ideation lanes; next add lane-level model/effort/policy controls and extend the same contract into execution-lane settings |
| P0 | Prompt / agent source of truth | Investigating | Evaluate replacing Claude-only prompt/plugin layout with centralized agent metadata that can generate Claude plugin assets and Codex-compatible spawn config from one source |
| P0 | Event normalization | In progress | Raw Codex `thread.started`, `agent_message`, `mcp_tool_call`, `command_execution`, error, and usage events now parse into the backend stream path, chat run/conversation payloads expose additive `provider_harness` / `provider_session_id` fields, core frontend lifecycle hooks now consume typed provider-aware run payloads, and provider-session compatibility normalization now lives in the shared domain conversation helper instead of being reimplemented in SQLite repos/payload helpers/transport response shaping; next migrate the remaining UI/render/settings surfaces off legacy Claude-only names |
| P0 | MCP / internal tools bridge | Investigating | Map how RalphX internal MCP currently reaches Claude and define the Codex equivalent, likely via HTTP/Streamable HTTP config injection rather than Claude plugin-dir semantics |
| P0 | RalphX native delegation bridge | In progress | Landed slices: provider-agnostic MCP tools (`delegate_start`, `delegate_wait`, `delegate_cancel`), backend-owned async job snapshots/cancellation, canonical-agent spawn selection, ideation child-session continuity, explicit parent turn/message lineage, and per-job status history so Claude and Codex can dispatch specialists through the same contract; next add richer live progress relay, non-ideation contexts, and prompt migration off Claude-native specialist assumptions |
| P0 | Recovery / capacity / reconciliation | In progress | Provider-neutral session refs now persist on conversations/runs, interactive turn completion writes provider refs, stale-session cleanup clears provider refs, queue resume branches by harness, interrupted-conversation discovery includes Codex rows, stale-session recovery/retry keeps the selected harness, and the remaining runtime harness-default / Claude-resume helper paths now source legacy compatibility from shared domain helpers; next wire startup/reconciliation/execution lifecycle fully off Claude-only assumptions |
| P0 | Future harness expansion architecture | In progress | Treat Claude/Codex as the first two entries in a longer-lived harness surface: `TaskTransitionService` execution wiring now uses a provider-neutral `AgentClientFactoryBundle`, `AppState` owns provider clients via a shared `AgentClientBundle`, the shared bundle layer now owns both the standard default+harness registration path and the domain-level `DEFAULT_AGENT_HARNESS` instead of repeating literal Claude+Codex maps/defaults in each constructor, shared domain harness helpers now also own the standard lane-default/fallback policy so config/bootstrap/runtime resolution stop carrying separate Codex default tables, execution spawner fallback now keys off that declared default harness instead of hardcoding Claude, transition/scheduler/runtime builders pass those bundles directly instead of replaying harness overrides by hand, `RuntimeFactoryDeps` now has grouped core/runtime helper constructors so startup/scheduler/merge/chat-handler/AppState callsites stop open-coding the same runtime bundle, the standard production/mock runtime client sets and factory sets now derive from one standard full-harness map and then split out `DEFAULT_AGENT_HARNESS` instead of maintaining separate default-vs-extra constructor paths, the standard runtime client/factory constructors in `agent_client_bundle` now also share one harness builder descriptor instead of parallel constructor `match` tables, `AgentClientBundle` now also owns the preferred-available-client fallback policy so callers stop re-implementing the non-default-harness availability check and fallback-to-default behavior locally, interactive process registry entries now also carry additive harness/session metadata so Gate 1 reused-process paths stop inferring provider details from stale conversation rows or default-harness fallbacks, handler-side stream-error recovery/failure payloads now also thread the effective harness instead of default-harness guessing when provider metadata has not been persisted yet, bundle callers now also have explicit `has_/explicit_harness_*` plus iterator accessors so `AppState`, `TaskTransitionService`, and future runtime code stop reaching into `harness_clients` / `harness_factories` directly, the spawner builder now also accepts harness-client iterators directly so transition/runtime callers stop replaying the one-by-one client registration loop by hand, the shared domain harness layer now also owns both `STANDARD_AGENT_HARNESSES` and a closure-based `standard_harness_registry(...)` helper so first-class harness registries stop re-declaring the same Claude/Codex `HashMap` shape in probes/resolvers/runtime-bundle builders/tests, the shared domain harness layer now also owns the standard harness behavior descriptor for team-mode/merge-watcher/effort/model-label/stream-protocol policy so app helpers stop encoding that behavior in their own harness `match` blocks, the application layer now also owns a shared harness runtime adapter registry for probe + chat-CLI + startup-integration resolution so availability/chat-context/server-boot code stop maintaining parallel per-harness registries, Codex diagnostics now also source probe + capability stitching from that shared runtime adapter layer instead of command-local parallel logic, default-harness runtime probing now also comes from that shared adapter layer instead of app-layer callers spelling `DEFAULT_AGENT_HARNESS` probes directly, the default chat-service bootstrap path now also comes from that shared runtime layer instead of `ClaudeChatService::new` hardcoding CLI/plugin-dir/root-resolution inline, the default external MCP startup bootstrap path now also comes from that shared runtime layer instead of `startup_background` hardcoding Claude config validation/plugin-dir entry lookup/node resolution inline, default external-MCP config/path/port command surfaces now also route through that shared runtime layer instead of command-local Claude helper calls, named runtime-layer accessors now also own external-MCP field reads like question wait timeout, ideation message queue cap, external-session similarity threshold, verification auto-verify enablement, verification max-round count, verification specialist lists, session-linking verification prompt limits, scheduler ready/merge settle delays, merge-reconciliation retry/timeout/circuit-breaker scalars, execution/review/QA retry/staleness/wall-clock scalars, merge-watcher/pending-merge timing fields, and execution retry-backoff scalars instead of handlers reaching through raw config structs directly, default repo-root working-directory and plugin-dir resolution for background ideation/project helper agents now also route through that shared runtime layer instead of command-local `current_dir().parent()` + `resolve_plugin_dir(...)` duplication, the default-harness helper-agent bootstrap for session namers and project analyzers now also routes through that shared runtime layer instead of per-caller plugin-dir/agent-role/env wiring, the legacy ideation model/effort settings command fallbacks now also route through shared Claude resolver helpers instead of reading Claude config directly, default UI feature flags plus verification/external-session startup timing config now also route through that shared runtime layer instead of command/startup code pulling those values directly from Claude config helpers, startup execution-default and harness-lane default seeding now also route through that shared runtime layer instead of `setup_settings` reading Claude config directly, smaller scheduler/reconciliation config callsites now also route through shared default config accessors instead of pulling those values straight from Claude runtime config, verification/question/external-ideation HTTP handlers now also route verification/scheduler/external-MCP defaults plus default repo-root/plugin-dir bootstrap through that shared runtime layer instead of handler-local Claude config lookups, artifact/session-linking/external-verification HTTP flows now also source verification defaults through that shared runtime layer instead of direct Claude verification config reads, Claude-only team HTTP spawn paths now also reuse the shared default-harness plugin-dir bootstrap so that boundary stays centralized without implying Codex team support, runtime callers/tests now also import probe types/helpers from that registry directly instead of piggybacking through `ideation_harness_availability` re-exports, startup harness integration resolution/execution now also flows through that shared adapter layer so `server_boot` no longer hardcodes Claude MCP registration directly, context-level runtime availability/team-mode checks now also share one resolver path instead of independently recomputing lane/project/probe state, chat-service launch planning now lives in `chat_service_context` instead of `mod.rs` hand-encoding Claude interactive vs Codex background behavior inline, launch-plan execution now also lives on `ResolvedChatHarnessLaunch` instead of `mod.rs` knowing which harness needs `spawn()` vs `spawn_interactive()`, launch-plan construction now also delegates through a resolved-CLI helper instead of keeping a third inline Claude-vs-Codex builder branch at the public entrypoint, noninteractive command/resume entrypoints now share resolved-CLI builder helpers instead of each repeating the Claude-vs-Codex command construction shape inline, the harness-specific noninteractive/launch builder behavior itself now lives on `ResolvedChatHarnessCli` instead of being re-matched in three separate helper functions, chat-service CLI resolution now also comes from an explicit harness-keyed resolver registry instead of a direct top-level `match`, chat-service interactive/background/resume command paths now share one harness-CLI resolver instead of re-probing Claude/Codex separately in each caller family, generic chat-service availability now probes `DEFAULT_AGENT_HARNESS` through the shared harness registry instead of checking only the stored Claude CLI path, lane resolution now treats non-default harness defaults generically instead of carrying a Codex-only default helper while preserving legacy fallback behavior for the default harness, the lane-availability core type/builder naming is now generic instead of ideation-only even though the existing ideation command surface stays stable, team-mode support checks in ideation/runtime availability now flow through that shared harness behavior helper instead of direct `== Claude` checks, default-harness availability errors/logging now describe the configured harness runtime instead of hardcoding `Claude CLI`, the standard harness probe registry now keys explicit harnesses (`claude`, `codex`) instead of smuggling Claude behind `DEFAULT_AGENT_HARNESS`, missing harness probes now fail explicitly instead of silently falling back to the default-harness probe, ideation lane-settings transport now accepts provider-neutral harness strings even though the current UI still offers Claude/Codex only, Codex-specific convenience aliases have been removed from the lower-level spawner/transition/AppState builder surface in favor of harness-keyed methods, ideation harness availability now probes through a small harness-probe registry/default-harness path instead of open-coding Claude/Codex special cases, and ideation background sidecars resolve non-default harnesses through that registry instead of a Codex-only branch; next keep converging the remaining fallback/runtime builders on that provider-neutral bundle shape so a third harness does not trigger another repo-wide split refactor |
| P0 | Documentation / merge readiness | In progress | Landed: `AGENTS.md`, `.claude/rules/multi-harness.md`, `.claude/rules/agent-mcp-tools.md`, `.claude/rules/mcp-servers.md`, task-execution rule wiring, the new user guide on agent harnesses, user-guide updates for getting started/configuration/ideation/orchestration/merge, plus README/session-recovery/web-testing copy now describe supported harnesses, lane-level selection, Codex solo-only behavior, harness-specific MCP/bootstrap semantics, legacy compatibility, and current parity limits; next finish any remaining merge-blocker product docs without reopening broad architecture-reference churn |
| P0 | Schema / compatibility migration | In progress | Provider-neutral DB/session metadata is landed and backend/frontend chat contracts now expose additive provider fields with Claude aliases preserved; next move recovery, UI renderers, and settings/profile contracts off Claude-only shapes |
| P0 | Lane settings matrix | Investigating | Define per-lane `harness + model + effort + approval + sandbox` settings for ideation, verifier, ideation subagents, verifier subagents, execution, review, re-execution, and merge-conflict handling across YAML, env, DB, and UI |
| P0 | Implementation planning | Completed | Concrete phase plan, migration plan, regression/test plan, implementation contract, and PR-sized backlog are in place; next execute the remaining runtime slices against those docs instead of reopening discovery |
| P0 | Phase-1 contract freeze | Completed | Additive provider-neutral session/run metadata, the `agent_lane_settings` storage model, normalized event payloads, and the phase-1 Codex solo-lane contract are frozen; next keep implementation aligned with the contract docs |
| P0 | Execution backlog | In progress | Execution/review/merge Codex support is required before merge, not future work; next execute the explicit `## Codex Merge Tracker` below in order and keep status current each turn |
| P1 | Model / effort defaults | Investigating | Default Codex to `gpt-5.4` with `xhigh` reasoning where supported; map Claude effort semantics vs Codex reasoning config and define per-phase overrides and downgrade rules |
| P0 | Execution pipeline compatibility | In progress | Audit and remove remaining Claude-only spawn/bootstrap/consumer assumptions for worker/reviewer/merger/runtime recovery flows before merge |
| P1 | Logging / auditability | Investigating | Standardize per-harness prompt capture, raw event logs, parsed event traces, provider session ids, and spawn metadata so failures remain debuggable across Claude and Codex |
| P1 | Regression coverage | Planned | Build compatibility tests that replay Codex/Claude raw events through the shared parser contract and cover settings, availability, recovery, queueing, UI payload transforms, and compatibility migrations |

## Codex Merge Tracker

| Priority | Stream | Status | Next Step |
|---|---|---|---|
| P0 | Execution / review / merge Codex parity | In progress | Codex must fully work for execution worker, review, re-execution, merge retry, and merge conflict handling lanes before merge; audit every remaining Claude-only runtime assumption and land owned regressions per lane |
| P0 | Harness-aware helper-agent bootstrap | Completed | Landed: helper-agent bootstrap is now harness-keyed, ideation/session/project helper-agent callsites use the effective runtime harness, and focused bootstrap regressions lock the behavior |
| P0 | Execution-side spawn/bootstrap parity | In progress | Landed first slice: worker/reviewer/merger execution spawners now use harness-keyed bootstrap/plugin resolution for Codex; next finish the remaining restart/recovery/bootstrap callsites and keep reviewer/merger Codex regressions alongside worker coverage |
| P0 | Recovery / resume parity | In progress | Landed slices: stale-session retry now preserves the full execution-side runtime dependency bundle (step repo, review repo, interactive registry, verification registry) instead of dropping back to a stripped retry path, conversation relaunch/runtime resolution now preserves a stored provider harness over mismatched lane config instead of silently re-deriving a different harness/model from newer lane rows, queued replay now carries `harness_override` through queue storage/relaunch so paused review/execution retries keep their chosen provider runtime, fresh execution/merge relaunches now inherit the parent conversation harness only for true recovery/startup/resume continuations without reusing the old provider session id, and paused resume continuations now mark both execution-command and task-command relaunches with `trigger_origin = "resume"` before re-running entry actions so lineage-preserving provider selection stays aligned across both surfaces; explicit retry/new-attempt paths still fall back to current harness settings unless they carry an explicit override; next audit resume/restart paths for any remaining harness-local recovery assumptions |
| P0 | Event / transport parity | In progress | Keep provider-neutral run/conversation payloads canonical, preserve legacy aliases, and eliminate remaining consumer paths that still infer Claude-only runtime behavior for execution-side flows |
| P0 | Validation / merge gate | Planned | Before merge, run a deliberate endgame matrix covering Rust backend, frontend type/tests, representative Codex capability tests, execution/review/merge regressions, and one final `cargo clean` in `src-tauri` |
| P1 | Docs / rules final sweep | In progress | Finish architecture/reference/doc wording so Codex multi-harness support, Claude-only team mode, and legacy compatibility are all explicit and current before merge |

### Concrete Merge Checklist

| Priority | Item | Status | Note |
|---|---|---|---|
| P0 | P0.1 Reviewer fresh-cycle harness semantics | Completed | Landed: fresh reviewer cycles now ignore stale stored provider harness lineage for spawn settings, while review-chat continuations still preserve stored provider session behavior |
| P0 | P0.2 Stopped restart semantics by lane | Completed | Landed: manual stopped restarts now have explicit proof coverage for the rule that reviewer fresh cycles use current settings, while task/merge fresh spawns without continuation metadata do not inherit parent provider lineage; this keeps worker, re-execution, and merger restarts on the new-attempt path unless an explicit continuation override exists |
| P0 | P0.3 Startup recovery semantics by lane | Completed | Landed: startup recovery now has explicit proof coverage that execution and merge fresh spawns inherit parent provider lineage, review recovery preserves the stored review harness, and the main interactive spawn path loads review task metadata so startup recovery can carry the continuation signal |
| P0 | P0.4 Merge retry and merge-conflict relaunch semantics | Completed | Landed: merge auto-retry and merge-conflict recovery paths preserve continuation lineage, and deferred/main merge scheduler retries now stamp `trigger_origin=\"recovery\"` before re-entering `PendingMerge`, so Codex merger relaunches do not drift to current settings mid-attempt |
| P0 | P0.5 Re-execution semantics after settings change | Completed | Landed: explicit retry/re-execution paths stay on the new-attempt path unless they carry an explicit override, with focused coverage that `trigger_origin=\"revision\"` does not inherit stale provider lineage for TaskExecution relaunches |
| P0 | P0.6 Event / transport parity recheck | Completed | Reconfirmed provider-neutral payloads and legacy aliases across backend/frontend transport surfaces with `cargo test --manifest-path src-tauri/Cargo.toml chat_service_types --lib`, `cargo test --manifest-path src-tauri/Cargo.toml --test unified_chat_commands`, `npm --prefix frontend run test:run -- src/hooks/useAgentEvents.test.tsx src/hooks/useGlobalAgentLifecycle.test.ts src/api/chat.test.ts`, and `npm --prefix frontend run typecheck` |
| P0 | P0.7 Endgame validation matrix | Completed | Landed endgame backend/frontend/Codex capability coverage with `cargo test --manifest-path src-tauri/Cargo.toml chat_service_types --lib`, `cargo test --manifest-path src-tauri/Cargo.toml --test unified_chat_commands`, `cargo test --manifest-path src-tauri/Cargo.toml interactive_runtime_tests --lib`, `cargo test --manifest-path src-tauri/Cargo.toml --test chat_session_recovery_integration`, `cargo test --manifest-path src-tauri/Cargo.toml spawner --lib`, the focused scheduler regressions for deferred/main merge retry lineage, the frontend transport tests, and `npm --prefix frontend run typecheck`; finish with a final `cargo clean` before handoff |
| P1 | P1.1 Docs / rules / user-guide final sweep | Completed | Final architecture wording now makes the provider-neutral harness boundary explicit in `docs/architecture/agent-chat-system.md`, `docs/architecture/ralphx-yaml-config.md`, `docs/architecture/claude-spawning-system.md`, and `docs/architecture/agent-catalog.md`; earlier multi-harness updates in `AGENTS.md`, `.claude/rules`, and user guides remain the current source of truth |

### Frontend Multi-Harness UX Tracker

| Priority | Stream | Status | Concrete Next Step |
|---|---|---|---|
| P0 | Chat header / panel context | Completed | Landed active harness and provider-session context in `frontend/src/components/Chat/IntegratedChatPanel.tsx` and `frontend/src/components/Chat/ChatSessionToolbar.tsx` so the selected conversation/run shows whether it is Claude vs Codex and whether it is a continuation vs a fresh attempt |
| P0 | Message-level provider rendering | Completed | Landed provider/harness badges and session lineage metadata in `frontend/src/components/Chat/MessageItem.tsx` via `frontend/src/components/Chat/provider-harness.ts` |
| P0 | Tool widget provider neutrality audit | Completed | Audited `frontend/src/components/Chat/tool-widgets/registry.ts` plus the visible widgets (`VerificationWidget.tsx`, `ReviewWidget.tsx`, `MergeWidget.tsx`, `ChildSessionWidget.tsx`, `SendMessageWidget.tsx`); no remaining Claude-shaped user copy required behavior changes, and the registry now documents that harness identity belongs in chat chrome rather than individual widgets |
| P0 | Conversation-switcher context | Completed | `frontend/src/components/Chat/ConversationSelector.tsx` now shows “stored session” vs “new attempt” routing hints alongside harness badges so users understand why a run stayed on Codex/Claude after settings changed |
| P0 | Execution pipeline settings UI | Completed | Split the harness entry points so execution worker/reviewer/re-executor/merger settings are a first-class execution-pipeline surface with clearer grouping/copy in `SettingsDialog.tsx` and `settings-registry.ts` |
| P0 | Execution lane policy controls | Completed | Execution-lane-specific model / effort / approval / sandbox / fallback controls are now exposed as a dedicated execution harness screen with execution-specific defaults/help text |
| P1 | Settings information architecture | Completed | The harness UX is now split into `Execution Agents` and `Ideation Agents` sections in `frontend/src/components/settings/SettingsDialog.tsx` and `frontend/src/components/settings/settings-registry.ts` |
| P1 | Frontend transport tests | Completed | Added assertions for the new chat header/message metadata and execution-settings UX changes on top of the existing provider-aware API/event coverage |
| P1 | Human-facing UX docs | Completed | Refreshed `README.md`, `docs/user-guides/agent-harnesses.md`, `docs/user-guides/configuration.md`, and `docs/user-guides/agent-orchestration.md` so users can find the execution-pipeline harness controls and understand the chat badges |

### Chat Widget Coverage Tracker

| Priority | Stream | Status | Concrete Next Step |
|---|---|---|---|
| P0 | Persisted chat payload contract audit | Completed | Audited the real stored/rendered contract: `chat_messages` persist `role`, `content`, `metadata`, `parent_message_id`, `tool_calls` JSON, and `content_blocks` JSON; provider lineage remains conversation/run metadata via `provider_session_id` and `provider_harness` |
| P0 | Widget registry coverage map | Completed | The registry is now explicitly audited against direct tests: uncovered families were `ArtifactWidget`, `ContextWidget`, `IssuesSummaryWidget`, `MergeWidget`, `ReviewWidget`, `SendMessageWidget`, `StepIndicator`, `StepsManifestWidget`, `TeamTaskWidgets`, and `McpContextWidgets` |
| P0 | Direct widget-family coverage | Completed | `frontend/src/components/Chat/tool-widgets/registry-coverage.test.tsx` now exercises the previously untested grouped widget families plus registry-level routing with representative parsed chat/tool payloads |

### Chat Render Architecture Tracker

| Priority | Stream | Status | Concrete Next Step |
|---|---|---|---|
| P0 | Render-layer map | Completed | The render stack is now explicitly identified as: raw persisted `ChatMessage` payloads → API parsing/transforms → `MessageItem` content-block rendering → `ToolCallIndicator` generic/task/diff/widget dispatch → specialized widget families |
| P0 | Shared chat render fixtures | Completed | `frontend/src/components/Chat/__tests__/chatRenderFixtures.ts` now provides canonical builders for raw ideation messages, parsed `ToolCall`s, `ContentBlockItem`s, message attachments, and `MessageItem` props so render tests stop inventing divergent shapes |
| P0 | High-churn render test migration | Completed | `ChatMessage.test.tsx`, `MessageItem.test.tsx`, `ToolCallIndicator.test.tsx`, and `tool-widgets/registry-coverage.test.tsx` now share the same canonical fixture builders for raw-payload, parsed-message, and widget-routing coverage |
| P1 | Legacy chat render duplication | Open | Decide whether `frontend/src/components/Chat/ChatMessage.tsx` remains an intentionally separate legacy ideation renderer or should converge toward the `MessageItem` render contract to reduce duplicate parsing/render rules |
| P1 | Test noise burn-down | Open | Burn down unrelated frontend suite warnings (`act(...)`, Radix dialog a11y warnings, noisy mocked query failures) so future chat/UI regressions have cleaner signal |

### Chat Replay E2E Tracker

| Priority | Stream | Status | Concrete Next Step |
|---|---|---|---|
| P0 | Real DB fixture taxonomy | Completed | Local `src-tauri/ralphx.db` sampling now identifies the real replay families: compact two-message worker/reviewer/merger runs plus long ideation orchestrator sessions with `Agent`, `TaskCreate`, `TaskUpdate`, `SendMessage`, MCP context, and `Read`/`Grep`/`Glob` blocks |
| P0 | Seedable mock chat scenarios | Completed | `frontend/src/api-mock/chat.ts` now exposes resettable DB-derived scenarios for `ideation_db_widget_mix`, `execution_db_compact`, `review_db_compact`, and `merge_db_compact` through `window.__mockChatApi` |
| P0 | Playwright chat replay fixture | Completed | `frontend/tests/fixtures/chat.fixtures.ts` now seeds chat replay scenarios into the web-mode mock API and invalidates React Query before navigating to the ideation chat surface |
| P0 | Ideation chat replay visual coverage | Completed | `frontend/tests/visual/views/ideation/ideation-chat-replay.spec.ts` now covers the DB-derived ideation conversation panel and snapshots mixed provider/widget rendering in the real `IntegratedChatPanel` path |
| P0 | Execution/review/merge chat replay visual coverage | Completed | `frontend/tests/visual/views/task-detail/task-chat-replay.spec.ts` now covers compact worker, reviewer, and merger DB-derived conversations through the real task-detail integrated chat path |
| P1 | Replay scenario module hygiene | Completed | DB-derived replay payloads now live in `frontend/src/api-mock/chat-scenarios.ts`, leaving `frontend/src/api-mock/chat.ts` focused on controller/state behavior instead of mixing API flow with large embedded fixtures |
| P1 | Replay fixture realism expansion | Open | Add one richer execution-side scenario with multiple assistant turns and mixed widgets once real review/merge replay needs exceed the current compact two-message seed set |

### Chat Widget Snapshot Matrix

| Priority | Widget Family | Status | Concrete Next Step |
|---|---|---|---|
| P0 | Proposal widget states | Completed | `frontend/tests/visual/views/chat/chat-widget-matrix.spec.ts` now snapshots create / update / delete proposal states via the `ideation_widget_matrix` replay scenario and `proposal-widget-*` selectors |
| P0 | Verification widget states | Completed | `frontend/tests/visual/views/chat/chat-widget-matrix.spec.ts` now snapshots update / get / pending verification states via the `ideation_widget_matrix` replay scenario and `verification-widget-*` selectors |
| P0 | SendMessage + ideation question states | Completed | `frontend/tests/visual/views/chat/chat-widget-matrix.spec.ts` now snapshots broadcast send-message plus question / plan-create / plan-update ideation states via `send-message-widget-*` and `ideation-widget-*` selectors |
| P0 | Review widget states | Completed | `frontend/tests/visual/views/chat/chat-widget-matrix.spec.ts` now snapshots approved complete-review and expanded review-notes states via the `review_widget_matrix` replay scenario |
| P0 | Merge widget states | Completed | `frontend/tests/visual/views/chat/chat-widget-matrix.spec.ts` now snapshots target / conflict / incomplete / complete merge states via the `merge_widget_matrix` replay scenario and `merge-widget-*` selectors |
| P0 | ChildSessionWidget visual states | Completed | `frontend/tests/visual/views/chat/chat-widget-matrix.spec.ts` now snapshots active / pending-capacity / loading / error child-session cards via the shared web-mode mock chat controller instead of brittle hook-only mocks |
| P1 | Execution file/search widgets | Covered via composite replay | Keep `execution_db_compact` task replay coverage for `Read`, `Grep`, and `bash`; only add isolated snapshots if layout churn starts escaping the current replay + unit tests |
| P1 | Context / artifact / team / MCP widgets | Covered via direct RTL coverage | Registry-level render coverage plus mixed replay is the current source of truth; promote specific isolated snapshots only if future visual churn shows those clusters are unstable |

### Chat Render Contract Tracker

| Priority | Area | Status | Concrete Next Step |
|---|---|---|---|
| P0 | Persisted chat message shape map | Completed | Replay fixtures now explicitly treat provider lineage as conversation-level metadata and widget rendering as message `content_blocks` / `tool_calls` driven, with the matrix scenarios sourced from DB-derived conversation patterns |
| P0 | Claude transcript shape matrix | In progress | Landed: the transcript parser now normalizes historical Claude project JSONL into a shared summary (`primary_model`, provider-profile classification, grouped assistant-turn count, aggregated usage, source path) while treating project-session and subagent envelopes as related but non-identical; sampled corpora include Anthropic Claude models plus profile-routed `glm-*`, `z-ai/*`, and `openai/*` variants, so next fold those parsed profile signals into first-class stored attribution instead of leaving them implicit in the source string |
| P0 | Header attribution truthfulness | In progress | Landed second UI slice: message badges now also use evidence-oriented tooltip copy (`Harness` / `Upstream` / `Profile` / `Session ref`) and no longer rely on continuity wording stronger than the persisted message/conversation facts; next audit any remaining task-history/banner copy that still implies stronger continuity than the stored data proves |
| P0 | Header layout + status placement | In progress | Landed first UI slice: task/review/merge chat headers now prioritize harness + model + effort + stats on the primary row and move live activity/response state to a secondary inline status row; next fix the remaining ideation empty-header spacer, restore single-row alignment for live `Agent responding` state where space allows, and keep the activity affordance hidden when the Activity page feature flag is disabled |
| P0 | Large-number stats formatting | Open | Shorten chat usage totals in the header popover (`k` / `m`) so high-token conversations do not overflow or dominate the compact stats cards |
| P0 | Codex tool-name widget routing | In progress | Landed third slice: shared canonicalization now also normalizes double-colon Codex tool names like `ralphx::get_merge_target` / `ralphx::start_step`, and both Codex plus Claude stream persistence now write ordered `content_blocks` snapshots during the live turn so dedicated widgets can hydrate from stored history instead of collapsing into raw aggregated text; next audit remaining raw-call cases like `ralphx::get_project_analysis` / `ralphx::get_memories_for_paths` and decide whether they need new widgets or should stay generic |
| P0 | Freshness-merge routing UX truthfulness | In progress | Landed first slice: merge chat widgets now key off the real `complete_merge` response contract, showing `Branch update applied` plus resume-routing copy when `new_status` returns to execution/review instead of falsely implying terminal merge completion; next audit task history/banners/timeline copy for the same distinction |
| P0 | Native attribution capture for new runs | In progress | Landed second slice: native assistant/run attribution now carries first-class provider-origin metadata in addition to harness/session/model/effort, with Codex captured as upstream `openai` and Claude best-effort inferred from the effective settings profile / settings payload (`anthropic`, `z_ai`, `openrouter`) at runtime; next tighten remaining direct message creators and extend the same provider-origin capture to conversation-level metadata instead of only runs/messages |
| P0 | Live-turn stats hydration | In progress | Landed first slice: chat streaming now emits `agent:usage_updated` when incremental usage changes are persisted, Claude mid-turn flushes now persist usage alongside content-block snapshots, and the frontend stats query invalidates live so the header popover can update before finalization; next extend the same contract to any remaining non-stream completion paths and tighten the still-null live app stats query path so fallback aggregation is no longer carrying the whole experience |
| P0 | Chat stats query null-path | In progress | Landed first data-flow slice: `ChatSessionToolbar` now synthesizes a minimal fallback conversation from the already-known `conversationId/contextType/contextId` and provider props when metadata hydration lags, so stats fallback no longer hard-depends on a fully loaded conversation row; next trace the remaining live-app null cases end-to-end and remove any last windows where the popover can still outpace backend/query hydration |
| P0 | Stats presentation readability | Open | Shorten large totals (`k` / `m`), hide empty stats chrome, and keep live status on the same primary header line when the header has enough space instead of forcing a mostly-empty second row |
| P0 | Message-level attribution persistence | In progress | Landed third slice: the frontend chat transport now preserves the persisted per-message attribution/usage fields and message rows consume those message facts before falling back to conversation-level metadata, surfacing harness / upstream / profile / model / effort / usage details without re-deriving them locally; next land historical backfill writes onto the same contract everywhere instead of overloading free-form JSON forever |
| P0 | Run/conversation attribution surface parity | In progress | Landed fourth slice: `chat_conversations` now persist first-class `upstream_provider` / `provider_profile` too, with native runtime writes and historical Claude backfill updating conversation-level origin alongside runs/messages; next expose aggregate surfaces and make the UI/header truthfulness rely on those persisted conversation facts instead of loose continuity assumptions |
| P0 | Native usage/cost capture for new runs | In progress | Landed third slice: Claude/Codex single-agent stream processing now persists native usage both on `agent_runs` and on the assistant `chat_messages` created from those turns (`input_tokens`, `output_tokens`, `cache_creation_tokens`, `cache_read_tokens`, `estimated_usd`); next make usage hydrate incrementally during the live turn instead of only feeling reliable at completion and capture partial/error-path usage where possible |
| P0 | Chat conversation stats popover | In progress | Landed third UI/testing slice: the chat header stats popover now invalidates on live chat/agent events, falls back to the currently rendered conversation/messages when the dedicated stats query returns `null`, and web-mode Tauri mocks now implement `get_agent_conversation_stats` so replay/Playwright coverage can exercise the real popover path instead of always returning `null`; next fix the remaining live-app null-query root cause, explain `Runs 0/0` / no-data states more clearly, and stop rendering the header stats container when there is nothing meaningful to show |
| P0 | Ideation empty-header chrome | In progress | Landed first slice: ideation no longer reserves the historical activity/status slot when the Activity page feature flag is disabled and there is no meaningful provider/live status to show; next collapse any remaining empty metadata chrome when the whole toolbar would otherwise be visual noise |
| P0 | Ideation session naming regression | In progress | Landed backend slices: external ideation now shares the deterministic default-title helper and the `ralphx-utility-session-namer` helper no longer inherits the main ideation lane runtime, so Codex ideation projects still use the stable default helper client for naming; next verify queued/deferred-start cases still hand off to the namer path correctly |
| P0 | Ideation session naming delivery path | In progress | Landed slices: the external create path no longer bypasses the service-level default title contract, and the external/apply/session command surfaces now all route session naming through the dedicated helper runtime; next follow queued and delayed-start prompt paths so the namer agent still runs consistently when the first send is deferred |
| P0 | MCP cancellation error surfacing | In progress | Landed first slice: the shared stream-error handler now suppresses explicit non-fatal MCP cancellation stderr (`user cancelled/canceled MCP tool call`) from persisted assistant transcript content while preserving existing `content_blocks`, and generic agent failures still append their error note; next audit whether other provider-specific MCP cancellation variants deserve the same treatment instead of surfacing as user-visible transcript text |
| P0 | Provider session artifact registry | Open | Centralize provider-side session artifact lookup (`~/.codex/sessions/**`, `~/.codex/session_index.jsonl`, `~/.claude/projects/**`) behind one provider-neutral helper/service so resume checks and recovery logic stop open-coding filesystem assumptions in separate places |
| P0 | Codex resume bootstrap duplication | Open | The live ideation MCP-error spam appears separate from the external transport bug: current Codex resume runs call `codex exec resume <session>` while also injecting a full synthesized session-history/bootstrap prompt, so audit whether that double-bootstrap is replaying stale session-state / cancelled-tool context back into later turns and add regressions before changing the resume contract |
| P0 | Shared stream finalization vs live snapshots | Completed | Cross-harness DB-backed regressions now prove both Codex and Claude persist one ordered tool lifecycle block in place through the shared snapshot/flush helpers (`text -> tool -> tool_result/completed -> text -> final flush`) with no duplicate widget rows or reordered content blocks |
| P0 | Stream persistence architecture audit | In progress | Landed the stronger cross-harness regressions the audit called for, so the remaining question is architectural cleanup rather than correctness: decide whether `chat_service_streaming.rs` should stay as one shared persistence layer with these regressions or be split behind a common stream-state abstraction later |
| P0 | Chat tree data-flow audit | Open | Map the full path from backend stream events and DB snapshots through API transforms, React query state, `IntegratedChatPanel`, `MessageItem`, `ToolCallIndicator`, and widget dispatch so the remaining rendering regressions are fixed at the correct layer and covered by E2E tests |

### Canonical Agent Config Tracker

| Priority | Area | Status | Concrete Next Step |
|---|---|---|---|
| P0 | Agent naming normalization | Open | Normalize the canonical agent inventory to one namespaced convention rooted at `ralphx-<domain>-...`: ideation agents become `ralphx-ideation-*`, execution agents become `ralphx-execution-*`, review agents become `ralphx-review-*`, chat agents become `ralphx-chat-*`, memory agents become `ralphx-memory-*`, and utility/analyzer agents follow the same explicit domain prefix; define the final mapping first, then migrate runtime aliases, config, docs, prompts, generated assets, MCP allowlists, and tests in compatibility-preserving cohorts instead of piecemeal renames |
| P2 | `agent.yaml` role field cleanup | Open | `agents/*/agent.yaml` `role:` is still used for runtime agent-handle/team-state config and catalog tests, but it does not drive chat message role semantics; audit whether it should remain canonical runtime metadata or move behind a narrower agent-kind field once the harness catalog settles |
| P1 | Insights usage aggregation APIs | In progress | Landed fourth slice: the same scoped usage/provenance contract now also renders at task level inside `TaskContextPanel` via `useTaskChatUsageStats`, so task detail views can expose AI usage without per-status duplicate plumbing; next expand the visualization quality and add richer task/project breakdown surfaces where the current compact card becomes insufficient |
| P0 | Claude-profile provider differentiation | In progress | Landed third slice: the parsed transcript profile families plus native Claude settings-profile inference now persist as first-class conversation/run/message metadata (`upstream_provider`, `provider_profile`) instead of living only inside source strings or model ids; next expose that distinction through API/UI/Insights aggregates and avoid collapsing Anthropic-vs-`z_ai` Claude traffic back into a generic harness label |
| P0 | Attribution schema contract | In progress | Landed sixth slice: first-class `upstream_provider` / `provider_profile` now exist on conversations, runs, and messages for both native forward capture and historical Claude import, with repo-level update APIs for attribution-only and usage-only repairs; next add aggregate/materialized surfaces so Chat UI and Insights do not have to reconstruct stats and provenance ad hoc |
| P1 | Shared render fixture builders | In progress | Keep consolidating `frontend/src/components/Chat/__tests__/chatRenderFixtures.ts` and replay-scenario builders so new widget coverage stops inventing bespoke payload shapes in every spec |
| P1 | Mixed long-form replay realism | Open | Add one richer execution/review replay with more than two assistant turns once real regressions show the compact worker/reviewer/merge seeds are no longer enough |

## High-Value Refactor Targets

| Priority | Area | Why It Matters | Next Step |
|---|---|---|---|
| P0 | Turn-by-turn runtime factory convergence | Multi-harness work is still exposing repeated scheduler/chat/transition assembly every turn; if this is not treated as an active priority, Codex parity will keep leaking across commands/handlers/recovery paths | Each turn that touches runtime wiring must first check whether an existing shared builder can absorb the change; if not, promote/create the helper before patching more leaf callsites |
| P0 | `src-tauri/src/application/task_transition_service.rs` | Oversized, high-churn orchestration root for execution/review/merge; every Codex parity slice keeps paying rebuild + context costs here | Split provider-aware chat/spawn rebuilding, merge/review corrective routing, and builder/factory wiring into support modules before more feature work piles onto the root |
| P0 | `src-tauri/src/application/chat_service/mod.rs` + `chat_service_*` builder seams | Multi-harness branching is spread across send/resume/recovery/queue paths, so duplication keeps reappearing when Claude/Codex behavior diverges; the core chat builder chain plus a `ChatRuntimeFactoryDeps` constructor/builder layer now live in `runtime_factory`, grouped deps enrichers cover the repeated runtime/ideation/context repo wiring, the main `send_message` harness spawn branch is now collapsed behind one helper, queue/recovery/recovery-retry now share harness-aware command builders from `chat_service_context`, launch planning now lives in `chat_service_context` instead of `mod.rs`, interactive/background/resume command paths now share one harness-CLI resolver, non-interactive Codex new-vs-resume paths now share one spawn-settings resolver, and run metadata/capability shaping now uses shared chat helpers instead of more inline Claude/Codex branches, but higher-level deps sourcing and some runtime branches still repeat too much | Keep shrinking raw `ClaudeChatService::new(...)` fallbacks, then keep converging the remaining queue/recovery/runtime branches onto the same helper/factory paths so chat-runtime assembly does not keep getting recopied across recovery/startup callsites |
| P0 | Service reconstruction across commands/handlers/recovery | The same scheduler/chat/transition assembly keeps getting rebuilt in Tauri commands, HTTP handlers, startup, reconciliation, and merge/review side paths, which makes Codex parity fragile and easy to miss in one caller family | Provider-aware factory helpers plus grouped `ChatRuntimeFactoryDeps` / `RuntimeFactoryDeps` builders now cover the common assembly path, and AppState-backed chat helpers no longer need post-build reattachment of app-handle / interactive-process wiring in the touched command+HTTP surfaces including execution resume/orchestrator flows; keep migrating remaining ad hoc reconstruction sites until runtime wiring is almost always sourced from those helpers |
| P0 | Harness client ownership / registry | Execution-side spawner/service injection, `TaskTransitionService` factory ownership, `AppState` client ownership, transition-runtime fallback construction, scheduler-held transition rebuilding, the standard default+harness client registration path, spawner fallback-harness decisions, and grouped runtime-deps assembly now share bundle-shaped provider-neutral inputs across fallback and direct AppState builders, but some chat/runtime factory inputs still mix that shape with legacy raw dependency surfaces | Keep converging remaining fallback builders and runtime factory inputs on one provider-neutral runtime/client bundle so lower-level orchestration only consumes injected harness clients and metadata |
| P0 | Harness-aware helper-agent bootstrap | Landed: helper-agent bootstrap is now harness-keyed in `application::harness_runtime_registry`, and ideation/session/project helper-agent callsites now route through the effective runtime harness instead of default-harness bootstrap assumptions | Keep future helper-agent/bootstrap work on that shared harness-keyed path so sidecars do not regress to Claude-shaped plugin-dir or env wiring |
| P1 | `src-tauri/src/lib.rs` startup composition root | The crate entrypoint now mixes Tauri wiring, shared state bootstrap, startup recovery choreography, background worker launch, recovery queue wiring, and command registration, which makes Codex-related startup changes noisy and high-risk | `lib.rs` and `application::app_setup` are now thin; keep them that way by treating new startup behavior as support-module work by default and only widening these composition roots when there is no cleaner boundary |
| P1 | `src-tauri/src/application/task_scheduler_service/mod.rs` | Scheduler now carries readiness selection, retries, contention logic, and transition-service construction; Codex lane threading increases coupling further | Extract retry/contention and transition-builder support so scheduler focuses on task selection/orchestration only |
| P1 | `src-tauri/src/commands/git_commands.rs` | Merge retry and merge-control flows still own a lot of runtime wiring and background-service assembly | Move background merge-retry builder/runtime setup behind shared helpers so Codex lane parity and transition rules do not need to be patched in command code |
| P1 | Provider-aware runtime factories (`AppState`, transition/chat fallbacks, scheduler fallbacks) | Current behavior depends too much on whether `AppState` is discoverable from an `AppHandle`; non-AppState fallbacks still need explicit parity work, and app/http/command surfaces plus nearby handoff comments still tend to leak concrete Claude-era ownership semantics even when they already route through multi-harness builders | Keep converging on one provider-aware factory path, thread explicit lane/runtime deps only where true fallback construction is unavoidable, and prefer provider-neutral app-layer names like `AppChatService` even for the concrete implementation, with legacy Claude naming left only as compatibility aliases where needed |

## Cross-Session Tracker Notes

| Tracker | Current Guardrails |
|---|---|
| Autonomous scope drift prevention | Accepted ideation sessions stay read-only; unrelated blocking failures should prefer spawning follow-up ideation sessions with first-class provenance; review should send back to revise before escalating when drift looks fixable; any future scope guard must allow necessary plan correction and adjacent files, not only exact initial file lists |
| Build / compile-coupling reduction | Favor extracting pure high-churn decision logic out of the root `src-tauri` crate over cosmetic handler/file-size cleanup; prefer mechanical moves first and small fix-up edits second; the goal is faster incremental validation, not just smaller files |

## Current TDD Rollout

| Milestone | Tests First | Implementation Files |
|---|---|---|
| M1 Admission gate | Landed: global/project ideation admission, verification-child counts-as-ideation tests, project-aware chat-service admission for task/review/merge, scheduler-side project-capacity skipping, startup recovery project-capacity checks, and merge-retry scheduler wiring; next broader regression on queue pressure / recovery-heavy flows | `src-tauri/src/application/chat_service/mod.rs`, `src-tauri/src/application/task_scheduler_service.rs`, `src-tauri/src/application/startup_jobs.rs`, `src-tauri/src/commands/execution_commands.rs`, targeted tests in `src-tauri/tests/` |
| M2 Global pause/stop semantics | Landed: persisted `ExecutionHaltMode` in `app_state`, startup task recovery suppression, startup ideation recovery suppression, command-side halt-mode persistence, stopped-aware status payloads, and explicit stopped UI/control-bar semantics | `src-tauri/src/commands/execution_commands.rs`, `src-tauri/src/application/startup_jobs.rs`, app-state repos/migrations/tests, execution UI/hooks |
| M3 Queue + startup recovery | Landed for slot-consuming contexts: paused sends queue instead of spawning, ideation continuations stay pending under the halt barrier, resume relaunches paused ideation + active task/review/merge queued work, and stop clears queued slot-consuming work | `src-tauri/src/application/chat_service/mod.rs`, `src-tauri/src/application/chat_service/chat_service_queue.rs`, `src-tauri/src/application/chat_service/chat_service_send_background.rs`, `src-tauri/src/commands/execution_commands.rs`, `src-tauri/tests/chat_service_pause_flows.rs` |
| M4 DB/settings backend | Landed: repo/domain/command/migration coverage plus YAML-seeded pristine-row bootstrap for `global_ideation_max`, `project_ideation_max`, `allow_ideation_borrow_idle_execution`; next move to UI/API controls | execution settings repos/commands, migrations, `ralphx.yaml`, API contracts |
| M5 Settings UI | Landed: global/project ideation allocation controls now flow through frontend schemas, API transforms, mocks, `App.tsx`, and `SettingsView`; next validate them against per-project admission, borrowing, and paused-queue fairness under heavier mixed load | `src/components/settings/SettingsView.tsx`, `src/api/execution.ts`, related schemas/transforms/tests |

## Allocation Rules

| Rule | Detail |
|---|---|
| Shared hard caps stay | Keep existing `global_max_concurrent` and per-project `max_concurrent_tasks` |
| New pipeline cap | Add `global_ideation_max` + `project_ideation_max`; verification child sessions count as ideation |
| Derived reserve | Execution reserve is derived (`max - ideation_max`), not separately edited |
| Borrowing | Ideation may borrow only idle execution capacity and only when no runnable execution work is waiting |
| Global pause | Stops active work and blocks new starts everywhere, but preserves resumability |
| Global stop | Stops active work and blocks new starts everywhere, and stopped work must not auto-resume on startup or via execution-bar resume |
