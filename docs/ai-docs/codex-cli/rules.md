# Codex Rules

Official docs: `https://developers.openai.com/codex/rules`

Snapshot notes:

- Codex exposes a first-class rules/customization surface distinct from `AGENTS.md`.
- This gives Codex a native counterpart to part of the current Claude prompt/rule layering story.

RalphX notes:

- A future single source of truth could generate both Claude plugin prompts and Codex rule/config assets.
- RalphX should avoid a second hand-maintained prompt tree if rules can be generated from shared metadata.
