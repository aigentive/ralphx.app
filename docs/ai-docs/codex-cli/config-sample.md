# Codex Config Sample

Official docs: `https://developers.openai.com/codex/config-sample`

Snapshot notes:

- The sample config is the best compact reference for realistic `config.toml` composition.
- It is especially relevant for model defaults, approvals, sandbox, MCP servers, and subagents.

RalphX notes:

- A RalphX Codex harness should be able to emit a deterministic sample-equivalent config for a run without requiring the user to hand-edit Codex settings.
